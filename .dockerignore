# Keep the API image build context small. Only serving/ + best_model.keras
# are needed; everything below is excluded from the Docker build.
.git
.gitignore
.superpowers/
.dockerignore
Dockerfile
docker-compose.yml

# Frontend (deploys to Vercel, not in this image)
web/

# Training notebook + local Python env
index.ipynb
venv/
serving/.venv/
**/__pycache__/
**/*.pyc
.pytest_cache/

# Docs and design artifacts
docs/
DEPLOY.md
README.md
